title: Socialist Split
new-page: true 
subtitle: The Socialists' disagreements with our policies have grown too severe.
tags: event
view-if: socialist_dissent >= 60
on-arrival: socialist_split = 1; socialist_strength = socialist_strength/2; workers_ip -= 5; unemployed_ip -= 5; workers_other += 5; workers_cp += 3; n_advisors -= 1 if levi_advisor == 1; levi_advisor = 0; rb_strength -= 300; cp_relation -= 5; rb_militancy -= 0.05 if rb_militancy >= 0.1; n_advisors -= 1 if seydewitz_advisor == 1; seydewitz_advisor = 0; n_advisors -= 1 if rosenfeld_advisor == 1; rosenfeld_advisor = 0; n_advisors -= 1 if siemsen_advisor == 1; siemsen_advisor = 0
max-visits: 1

= Socialist members split

Our policy choices have greatly offended the socialist, and many of their leaders have split to form a new party, the SAPD, or Socialist Worker's Party. They do not count a huge section of our membership, so this should be survivable.

[? if centrists_resign = 1 : Many former Centrist Marxists have joined with the SAPD, the party formed by dissident socialistists. They have brought their support base and organization skills to the party, leading to its growth as a nascent rival to the ip for the support of the working class. ?]

